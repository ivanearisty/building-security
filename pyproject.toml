[project]
name = "building-security"
version = "0.1.0"
requires-python = ">=3.11"

[dependency-groups]
dev = ["ruff"]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.mypy]
python_version = "3.11"
strict = true
